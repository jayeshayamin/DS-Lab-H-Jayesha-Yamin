#include <iostream>
using namespace std;
int main() {
    int r,c;
    cout<<"Enter size of rows: ";
    cin>>r;
    cout<<"Enter size of columns: ";
    cin>>c;
    int** arr=new int*[r];
    for(int i=0;i<r;i++)
    {
        arr[i]=new int[c];
    }
    for(int i=0;i<r;i++)
    { for(int j=0;j<c;j++){
        arr[i][j]=0;}
    }
    for(int i=0;i<r;i++)
    { for(int j=0;j<c;j++){
        cout<<arr[i][j];}
        cout<<"\n";
    }
    int n,m;
    while(1){
       cout<<"\nEnter row and  column to fill.\n";
    cin>>n;
    cin>>m;
    //chooose out of bound n and m to immediately get final seat chart
        if((n>=0&&n<r)&&(m>=0&&m<c))
        {
    
    arr[n][m]=1;}
    else
    {
        cout<<"updated chart: \n";
    for(int i=0;i<r;i++)
    { for(int j=0;j<c;j++){
        cout<<arr[i][j];}
        cout<<"\n";
    }
    for(int i=0;i<r;i++)
       delete[] arr[i];
        delete[] arr;
    return 0;
    }
    }
}
    
